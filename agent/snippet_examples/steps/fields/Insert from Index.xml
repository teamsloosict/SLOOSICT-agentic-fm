<?xml version="1.0"?>
<fmxmlsnippet type="FMObjectList">
  <Step enable="True" id="0" name="Insert from Index">
    <SelectAll state="True"/>
    <Field table="" id="0" name=""/>
  </Step>
</fmxmlsnippet>
<!-- SelectAll state: "True" replaces entire field contents. "False" inserts at cursor position or appends to end. -->
<!-- Optional: SelectAll, Field (target field of any type). If Field is omitted, the active field is used; returns an error if field not on layout. -->
<!-- Displays the View Index dialog so the user can select an entry. Works in Browse and Find modes. -->
<!-- FileMaker Pro only; not supported on Go, WebDirect, Server, Cloud, Data API, or CWP. -->
<!-- Returns an error if indexing is off and "Automatically create indexes as needed" is deselected. -->
<!-- See https://help.claris.com/en/pro-help/content/insert-from-index.html -->
